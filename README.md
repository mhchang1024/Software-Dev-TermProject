# final-project-runtime-terror-the-trilogy
final-project-runtime-terror-the-trilogy created by GitHub Classroom
Instructions when cloning:
1. In order to run make sure to do the command "npm install" in the front-end directory
2. Once npm is installed do the command make sure to run the sparkdemo file in the back-end directory and then do the command "npm start" in the front-end directory
3. Finally, the program will automatically open your default browser and you should be able to use the website
